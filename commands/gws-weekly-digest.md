---
description: Weekly summary — this week's meetings + unread email count
argument-hint: [extra flags passed through to gws workflow +weekly-digest]
allowed-tools: Bash(gws workflow +weekly-digest:*)
---

Run the weekly-digest workflow. Present the result as a single short brief:

- Meetings this week (count + notable ones)
- Unread email count + breakdown by importance if available
- Anything that needs attention before EOW

!`gws workflow +weekly-digest $ARGUMENTS`
