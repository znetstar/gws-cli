---
description: Show today's calendar agenda across all calendars
argument-hint: [extra flags passed through to gws calendar +agenda]
allowed-tools: Bash(gws calendar +agenda:*)
---

Show the user's upcoming events. Run the agenda helper and present the result as a clean list (time, title, attendees count). Flag any conflicts (overlapping events) at the top.

!`gws calendar +agenda $ARGUMENTS`
